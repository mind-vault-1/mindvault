# Rust's output directory
target

# Local settings
.soroban
.stellar

# Test snapshots generated by Soroban SDK during tests
**/test_snapshots

# WASM size trend baseline, written by CI (see contract-ci.yml)
.wasm-size-baseline
